# Appunti di Calcolatori elettronici

Questi appunti sono in **Markdown**, pubblicati con **GitHub Pages**.

## Come contribuire / modificare
- Apri una pagina in `docs/` e modifica il file `.md`
- Aggiungi immagini e video in `docs/assets/`

## Esempi rapidi

### Formula (LaTeX)
Inline: $E = mc^2$

Blocco:

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

### Codice
```c
#include <stdio.h>
int main() {
  printf("Hello appunti!\n");
  return 0;
}
```
